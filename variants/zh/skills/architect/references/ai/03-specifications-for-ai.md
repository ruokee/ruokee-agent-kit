# 面向 AI 的规格（Specifications for AI）

把架构约束落成持久形式，并交给运行时或调用方加载；能自动验证的，再下沉为检查或门禁。适用于 AI 协作开发（vibe coding、agent 编码）中守住架构边界：决策记录、AGENTS.md 类规则文件、适应度函数的分层落位。

## 判断不写下来，等于没有

代码 AI 来产，但规格、约束、验收标准是人的——那该怎么给到 AI？聊天时说一句让它记得加幂等，行不通：这一轮记得，下一轮上下文压缩后忘了；换会话从零开始；换同事完全不知道。

更要命的是 AI 默认给乐观路径：你不明确写出退款要幂等、要防重复扣款，它就不写，产出一段 demo 能跑、生产会炸的代码。口头的、一次性的、靠人记的约束，在 AI 协作里等于不存在。架构约束必须落成持久形式。运行时支持并配置后自动加载，否则由调用方显式注入；能自动验证的，尽量落实为检查或门禁。

## 规格金字塔

约束按可执行程度分三层，越往下越硬、越绕不过：

|层级|载体|约束什么|给谁看|
|-|-|-|-|
|决策记录|使用项目现有格式的文档|为什么这么定、取舍|人 + AI（理解意图）|
|AGENTS.md 类规则文件|项目根的常驻规则|做什么 / 不做什么|AI（运行时支持并配置后自动加载，否则由调用方显式注入）|
|适应度函数 / lint / CI|自动化测试|红线（违反即拦）|机器|

三层各管一段缺一不可：有的约束只能靠人理解（为什么当年选了双写），写进决策记录；有的能用自然语言让 AI 持续遵守（退款必须幂等），写进 AGENTS.md；有的能被机器精确验证（领域层不准 import web 层），写成 CI 检查。**能往下沉一层，就别停在上一层**——文档靠自觉，CI 靠强制。

## 怎么写好规则文件

`AGENTS.md` / `CLAUDE.md` 放项目根，运行时支持并配置该机制时每次对话自动加载，是 AI 的长期记忆。多数人写成正确的废话：

```
- 写高质量、可维护、可扩展的代码
- 遵循最佳实践
- 注意性能和安全
```

AI 看到这些什么也做不了——高质量到底多高？最佳实践是谁的？有用的写法要具体、可执行、带为什么、有反例：

```
## 数据与一致性
- 凡是动钱的接口（扣款、退款）一律携带幂等键；同一请求再来一次，
  返回上次的处理结果，绝不二次执行。背景见 ADR-003。
- ❌ 不许把支付 API 直接交给模型调用；模型的产出止步于退款提议，
  真正动手的是确定性的退款服务。

## 韧性
- 所有外部调用（模型 API / 第三方）必须设超时；只对可恢复错误，
  在幂等、时间预算和重试预算内退避重试。
  理由：依赖一定会抽风；但永久错误、认证失败、配额耗尽和
  不可幂等的副作用调用不应自动重试。

## 边界
- 领域层禁止 import web 层 / 框架。违者 CI 会 fail。
```

五条纪律：

1. **具体到可执行。** 不说"注意安全"，说所有外部输入入库前必须校验、用户内容当不可信输入（含提示注入）。
2. **带上为什么。** 像 ADR 一样给理由——AI 和人知道理由，才不会在规则显得多余时把它删掉。
3. **给反例（❌）。** 明确禁止做什么比写应该做什么更管用，这正是堵住 AI 走乐观路径默认值的地方。
4. **短而狠。** 规则文件是常驻上下文成本，按 token 计费。写最高频、最容易被违反的那几条，别写成小说。
5. **随代码演进。** 过时的规则比没有更糟（它主动误导 AI），规则变了就改。

设计时做的每个选了 A 放弃 B 的取舍、写的每条决策记录，凡是希望 AI 持续遵守的，都该提炼成规则文件里的一条。AGENTS.md 就是写给 AI 的架构边界。

## 能机器强制的，就别只写文档

金字塔最底层最硬。约束的归宿按能不能被机器验证分流：

|约束|落成什么|
|-|-|
|领域层与框架解耦|CI 依赖检查不过即拦截|
|退款接口要求幂等|规则写进规则文件，契约测试缺幂等键即失败|
|模型调用只准走抽象层|依赖检查发现直连 provider 即拦截|
|p99 上限 200ms|性能测试超标即失败|
|为什么用 RAG 不用微调|决策记录（只能人读，无法机器验证）|

AI 会忠实执行你写下的约束，也会忠实无视你没写下的约束。把靠提醒的约束变成由红灯兜底的约束——CI 门禁不在乎代码是人还是 AI 提交的，一视同仁地拦。

## 规格即架构

在 AI 时代，把约束写清楚、写到 AI 读得到的地方，不是写文档的杂活，是核心架构工作本身。**你写下的约束 = AI 产出的边界。约束写得含糊，AI 就在含糊处替你做主，且总是挑最省事的乐观路径。** 架构师的判断力，通过规格这个接口，变成 AI 的行为约束。

## 与其他文档的关系

- 决策记录（ADR）的写法见[架构决策](../07-architecture-decisions.md)。
- 适应度函数防架构腐化的完整做法见[演进与迁移](../12-evolution-and-migration.md)。
- 规格管事前，AI 产出的事后审查见[审查 AI 产出](./04-reviewing-ai-output.md)。
